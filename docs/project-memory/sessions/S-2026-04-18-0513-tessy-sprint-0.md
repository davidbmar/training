# Session S-2026-04-18-0513-tessy-sprint-0

**Title:** TESSY Sprint 0 — feasibility gate (Qwen3.5 MLX runtime + smoke test)

**Goal:** Verify every load-bearing assumption of the TESSY implementation plan
(`~/.claude/plans/ok-look-at-this-misty-beacon.md`) before scaffolding any
downstream pipeline code. Produce a measured GO / NO-GO recommendation per
model role (teacher, 4B / 2B / 0.8B students) and per thinking-mode toggle.

**Context:** First pass implementing the approved TESSY plan after Codex
review. Sprint 0 scope is strictly limited to:

- `requirements-tessy.txt` (pinned dependency set)
- `scripts/tessy/README.md` (repo-native env setup docs)
- `scripts/tessy/extract_prompts.py` (prompt-only extractor)
- `scripts/tessy/smoke_teacher_only.py` (5-row smoke with strict JSON parsing)
- this feasibility report

No Sprint 1–5 files were created.

## Environment

| Component        | Value                                                   |
|------------------|---------------------------------------------------------|
| Platform         | Darwin arm64 (macOS 26.4.1)                             |
| Hardware         | Apple Silicon, 36 GB unified memory                     |
| Python           | 3.14.3 (Homebrew, `/opt/homebrew/opt/python@3.14/…`)    |
| mlx              | 0.29.3                                                  |
| mlx-lm           | 0.31.2                                                  |
| transformers     | 4.57.6                                                  |
| huggingface_hub  | 0.36.2                                                  |
| Free disk        | 29 GB start → 24 GB after 4B+2B+0.8B weights            |

Metal backend initialized cleanly. (Codex review noted a prior Metal init
crash — not reproducible with these versions.) `requirements-tessy.txt`
pins the system versions so `python3 -m venv .venv-tessy && pip install -r
requirements-tessy.txt` reproduces this environment. Smoke runs used system
Python to avoid a multi-GB venv install inside Sprint 0.

## Qwen3.5 artifact-name verification

Verified against the official `Qwen` org and `mlx-community` collections on
Hugging Face (April 17–18 2026):

| Role                 | Upstream repo           | MLX 4-bit repo                            | Download size |
|----------------------|-------------------------|-------------------------------------------|---------------|
| Teacher              | `Qwen/Qwen3.5-9B`       | `mlx-community/Qwen3.5-9B-MLX-4bit`       | ~5 GB |
| Student 4B           | `Qwen/Qwen3.5-4B`       | `mlx-community/Qwen3.5-4B-MLX-4bit`       | 2.9 GB |
| Student 2B           | `Qwen/Qwen3.5-2B`       | `mlx-community/Qwen3.5-2B-MLX-4bit`       | 1.6 GB |
| Student 0.8B         | `Qwen/Qwen3.5-0.8B`     | `mlx-community/Qwen3.5-0.8B-MLX-4bit`     | 0.62 GB |
| Boundary backbone    | `Qwen/Qwen3.5-0.8B-Base`| (no MLX 4-bit at this time)               | — (Sprint 1) |

**Thinking mode is runtime-toggled, not a separate repo.** The tokenizer
chat template exposes `enable_thinking=True|False`:

- `enable_thinking=True` — prompt ends with `<|im_start|>assistant\n<think>\n`
  (opening tag pre-filled, model continues inside the thinking span)
- `enable_thinking=False` — prompt ends with
  `<|im_start|>assistant\n<think>\n\n</think>\n\n` (closed empty thinking
  block, model generates the answer directly)

Token IDs: `<think>`=248068, `</think>`=248069, `<|im_start|>`=248045,
`<|im_end|>`=248046.

**Implication for the plan:** the strict JSON extractor in
`smoke_teacher_only.py` handles the "opening `<think>` in the prompt, not in
the output" case — when the generated text contains `</think>` without a
preceding `<think>`, everything up to and including the close tag is treated
as thinking and discarded. Unit-tested locally before running the first
smoke.

## Smoke-test methodology

Each run uses 5 prompts from `data/tessy/prompts/train.jsonl` (produced by
`extract_prompts.py`; 908 / 151 / 151 rows, assistant turn preserved as
`reference`). Temperature 0.6, top_p 0.95 (paper values). JSON extraction:
strip `<think>…</think>` (including the opened-in-prompt case), then scan
for the last brace-balanced span that parses as a dict, then validate the
`{slot_updates, confidence, response_draft}` schema. `max_tokens` varies by
mode: 8192 for thinking-on (so we could see how long thinking actually
runs), 2048 for no-thinking (plenty for one-to-two-sentence answers).

## Measured results

All numbers are from today's runs, 5 prompts each, saved to
`data/tessy/sprint0_report.jsonl`.

### Thinking mode ON (paper's default)

| Model   | Gen tok/s | Prompt tok/s | Peak mem | Wall/row | JSON valid | Notes |
|---------|-----------|--------------|----------|----------|------------|-------|
| 0.8B    | 102       | 2 641        | 1.14 GB  | 78–84 s  | **2/5 (40 %)** | all 5 hit 8192 length cap |
| 2B      | 61        | 1 342        | 1.89 GB  | 129–143 s | **3/5 (60 %)** | all 5 hit 8192 length cap |
| 4B      | 32        | 544          | 3.3 GB   | 191–257 s | **3/5 (60 %)** | 2/5 hit 8192 length cap |
| 9B      | _(pending — in flight at time of writing)_ |

Failure mode is consistent: Qwen3.5's free-form thinking trace ("Thinking
Process: 1. Analyze the request… 2. Decide the tone…") burns 4 000 – 8 000+
tokens for a one-sentence phone answer. When it doesn't hit the budget,
extraction works cleanly.

### Thinking mode OFF (`--no-thinking`)

| Model   | Gen tok/s | Prompt tok/s | Peak mem | Wall/row | JSON valid | Notes |
|---------|-----------|--------------|----------|----------|------------|-------|
| 0.8B    | 115       | 2 740        | 1.14 GB  | 0.8–1.2 s | **1/5 (20 %)** | fast, but too weak without reasoning |
| 2B      | 71        | 1 342        | 1.9 GB   | 0.9–3.0 s | **3/5 (60 %)** | viable phone latency |
| 4B      | 35        | 478          | 3.3 GB   | 4.1–4.7 s | **5/5 (100 %)** | best measured combination |
| 9B      | 22        | 292          | 5.88 GB  | 4.4–9.1 s | **4/5 (80 %)** | teacher; acceptable offline throughput |

### Bottom-line latency vs quality

![matrix]

| Model × mode    | JSON valid | Wall/turn | Phone-usable? |
|-----------------|-----------|-----------|---------------|
| 0.8B thinking   | 40 %      | ~80 s     | no            |
| 0.8B no-think   | 20 %      | ~1 s      | quality too low |
| 2B thinking     | 60 %      | ~135 s    | no            |
| **2B no-think** | **60 %**  | **~2 s**  | **yes, borderline** |
| 4B thinking     | 60 %      | ~236 s    | no            |
| **4B no-think** | **100 %** | **~4.5 s**| **yes, with streaming TTS** |
| 9B no-think     | 80 %      | ~6.3 s    | teacher only — too slow for phone, but fine for offline TESSY data synthesis |

## Key findings

### 1. Thinking mode is mechanically incompatible with phone-agent latency

For the phone-receptionist task (system prompt constrains the final reply to
"one to two short sentences"), Qwen3.5 thinking traces are **4 000–8 000+
tokens** — orders of magnitude larger than the actual answer. At measured
generation throughput, this translates to ≥80 s per turn on the smallest
student and ≥250 s per turn on the 4B student. Far above the < 2 s target.

This invalidates the plan's implicit assumption that TESSY's
"capability-span = thinking-reasoning" split is the right factorization for
this repo's domain. The paper tested TESSY on code generation, where
thinking is load-bearing; in slot-filling phone turns, thinking is bloat.

### 2. Thinking-mode OFF + larger student is the phone-viable path

The 4B student with thinking disabled hits **100 % JSON validity** on 5/5
smoke prompts at **~4.5 s / turn**. That's above the bare "feels
instantaneous" threshold but within the range streaming TTS can hide, and
well under "caller gets awkward." The 2B no-thinking student holds 60 %
validity at ~2 s / turn — acceptable if the training loop lifts its JSON
validity higher.

### 3. 0.8B is likely unusable as a student here

Even with no thinking (~1 s / turn), 0.8B hit only 20 % JSON validity on the
smoke. This matches the plan's risk entry — 0.8B may simply lack the
capacity to absorb teacher reasoning in any form. Unless it reaches ≥ 85 %
validity after SFT (big lift from a 20 % base), drop it from the planned
three-way comparison.

### 4. Plan revision: re-scope TESSY's capability-vs-style split

The plan's Capability Token definition ("thinking-trace reasoning steps +
slot_updates JSON + confidence + intent decisions") assumed thinking was
the dominant reasoning surface. Given no-thinking is the phone-viable mode,
the revised split should be:

- **Capability tokens:** keys and values in `slot_updates`, the numeric
  `confidence`, the JSON structural tokens (`{`, `}`, `"`, `:`)
- **Style tokens:** the `response_draft` prose

This pushes the Sprint 1a design decision strongly toward the **heuristic
parser** option: JSON structure vs prose is trivially separable with a
regex — no trained token classifier needed. Recommend dropping Option C
(custom MLX classifier head) and making Option A (heuristic) the default.

### 5. MLX-LM 0.31.2 handles Qwen3.5 without patches

Despite the mlx-community README mentioning Qwen3.5 support on a branch of
`mlx-vlm`, all three MLX 4-bit conversions load and stream correctly in
`mlx-lm` 0.31.2's `stream_generate`. No code patch or special flags
required. Prompt processing is excellent (500 – 2 700 tok/s).

## GO / NO-GO per role

| Role              | Verdict | Rationale |
|-------------------|---------|-----------|
| MLX runtime       | **GO**  | Metal init clean, Qwen3.5 loads and streams |
| JSON extractor    | **GO**  | 100 % on 4B no-thinking; failures trace to model, not parser |
| Prompt pipeline   | **GO**  | `extract_prompts.py` round-trips 908 / 151 / 151 rows with zero skips |
| Qwen3.5-9B teacher (no-think) | **GO as offline teacher** | 80 % base validity, 22 tok/s, 5.88 GB peak. ~6 s/turn × 908 prompts ≈ 1.5 h for a full teacher-only synthesis pass — acceptable for offline data gen. Not viable for online phone inference. |
| 4B student (no-think) | **GO** | 100 % valid, 4.5 s/turn. Best measured configuration. |
| 2B student (no-think) | **CONDITIONAL GO** | 60 % base validity; promising but needs SFT to lift. 2 s/turn fits phone. |
| 0.8B student (no-think) | **NO-GO** (recommend drop) | 20 % validity; insufficient capacity absent a major surprise after SFT |
| Thinking-mode TESSY (any size) | **NO-GO for phone production** | ≥80 s/turn; only usable for offline data synthesis, and 4–8 k-token "boilerplate" thinking adds no domain value for slot-filling |

## Recommended next actions before Sprint 1

**Plan updates (before Sprint 1):**

1. **Revise "Capability vs style definition"** — move to JSON-vs-prose
   factorization for no-thinking mode. (Capability = `slot_updates` keys /
   values + `confidence` + JSON structure; style = `response_draft` prose.)
2. **Revise Sprint 1a** — make the **heuristic parser** the default
   boundary predictor. JSON structure vs prose is trivially separable with
   a small regex parser; no trained token classifier needed. Drop the
   custom-head MLX option; drop the LLM-as-labeler fallback unless the
   heuristic surprises on edge cases.
3. **Revise the student sweep** — train 4B and 2B; drop 0.8B unless Sprint
   1 surfaces a cheap lift that makes 20 % → 85 % validity plausible.
4. **Teacher role** — lock in Qwen3.5-9B-MLX-4bit with `enable_thinking=False`
   as the offline TESSY teacher. 91-minute full-corpus pass is acceptable.
5. **Sprint 2 generation loop** — the alternating teacher/student handoff
   stays the same, but boundary decisions operate on JSON-vs-prose rather
   than thinking-vs-answer.

**Do not yet create:**

- Any Sprint 1+ files. Sprint 1 kicks off after the plan is updated per the
  findings above.

## Artifacts produced

- `requirements-tessy.txt`
- `scripts/tessy/README.md`
- `scripts/tessy/extract_prompts.py`
- `scripts/tessy/smoke_teacher_only.py` (supports `--no-thinking`,
  `--max-tokens`, `--report`)
- `data/tessy/prompts/{train,valid,test}.jsonl` — 908 / 151 / 151 rows
- `data/tessy/sprint0_report.jsonl` — machine-readable per-run summaries
- this session doc

## 9B teacher results

`mlx-community/Qwen3.5-9B-MLX-4bit` with `--no-thinking`, 5 prompts from
`data/tessy/prompts/train.jsonl`, `max_tokens=2048`, temperature 0.6:

- Load time: **264.4 s** first run (pulled ~5 GB of weights)
- Prompt processing: **292 tok/s** avg
- Generation: **22 tok/s** avg
- Peak GPU memory: **5.88 GB**
- JSON validity: **4 / 5 (80 %)** — the one failure produced `{slot_updates:…}` with a structurally valid JSON but missing the required keys (model chose to omit some keys rather than use the `"not collected"` sentinel).
- Wall time per row: **4.4 – 9.1 s**; full 5-prompt run: **31.67 s**.

Implied throughput for full-corpus teacher-only synthesis: 908 prompts × ~6 s/prompt ≈ **91 minutes**. Acceptable for an offline data-gen pass.

9B with thinking ON was **not measured** for Sprint 0. Given the consistent pattern at 0.8B / 2B / 4B (4–8 k-token free-form boilerplate followed by a ~40-token answer), thinking-mode 9B would likely take ≥5 minutes per prompt, or ~76 hours for the full 908-prompt corpus. If future work requires thinking-mode teacher synthesis, budget for an overnight batch — or drive the teacher's thinking harder with prompt engineering / a lower temperature.

## Commits

- (Sprint 0 commit to be created after 9B results land. Subject line will
  describe the Sprint 0 feasibility work; body carries
  `Session: S-2026-04-18-0513-tessy-sprint-0`.)

## Links

- Plan: `~/.claude/plans/ok-look-at-this-misty-beacon.md`
- Paper: https://arxiv.org/abs/2604.14164
- Qwen3.5 org: https://huggingface.co/Qwen
- MLX conversions: https://huggingface.co/mlx-community
